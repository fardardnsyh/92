import {
  Library,
  Settings,
  List,
  type IconNode as LucideIcon,
} from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  library: Library,
  settings: Settings,
  list: List,
};
